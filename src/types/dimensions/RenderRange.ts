import type { Range } from "./Range";

/**
 * Represents the render range of a {@link VirtualizedList}.
 *
 * Note: The coordinates start from the top-left corner of the list visible area (i.e. the scrollable container).
 */
export type RenderRange = Range;
