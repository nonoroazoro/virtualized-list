import type { VirtualizedListConfig } from "./VirtualizedListConfig";

/**
 * Represents the options for the virtualized list.
 */
export type VirtualizedListOptions = Partial<VirtualizedListConfig>;
