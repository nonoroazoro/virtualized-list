/**
 * Specifies whether the debug mode is enabled.
 *
 * If enabled, it will adds verbose console logging for e.g. data updates, list updates, and reconcile stuff.
 */
export const ENABLE_DEBUG_TRACING = false;
